# 이 파일을 .env로 복사한 후 값을 채우세요 (.env는 git에서 제외됩니다)
#
# 하나의 프로세스는 하나의 환경만 담당합니다.
# sandbox로 띄우려면 SWEETBOOK_ENV=sandbox + SWEETBOOK_API_KEY=<sandbox 키>
# live로 띄우려면    SWEETBOOK_ENV=live    + SWEETBOOK_API_KEY=<live 키>

SWEETBOOK_ENV=sandbox
SWEETBOOK_API_KEY=
PORT=8090
